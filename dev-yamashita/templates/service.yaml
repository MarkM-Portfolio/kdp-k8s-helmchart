apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-yamashita.name" . }}
  labels:
    {{- include "dev-yamashita.labels" . | nindent 4 }}
  namespace: dev
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ .Values.service.port | quote }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
  selector:
    {{- include "dev-yamashita.selectorLabels" . | nindent 4 }}
