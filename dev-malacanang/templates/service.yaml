apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-malacanang.name" . }}
  labels:
    {{- include "dev-malacanang.labels" . | nindent 4 }}
  namespace: dev
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ .Values.service.port | quote }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
  selector:
    {{- include "dev-malacanang.selectorLabels" . | nindent 4 }}
